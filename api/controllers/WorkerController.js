/**
 * WorkerController
 *
 * @description :: Server-side logic for managing Workers
 * @help        :: See http://links.sailsjs.org/docs/controllers
 */

module.exports = {
  /*create: function(req, res){
    var params = req.params.all()

    Worker.create({name: params.name}).exec(function createCB(err,created){
      return res.json({
        notice: 'Created worker with name ' + created.name
      });
    });
  }*/

};

